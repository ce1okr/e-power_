# EmpoweredSMP

A custom 8-ability RPG-class plugin for Paper 26.2 (Minecraft Java 26.2), built from your spec.

## Building

Requires **JDK 25** and an internet connection (to pull `paper-api` from PaperMC's Maven repo).
